import Config from 'react-native-config';

const {
	MOVIEDB_BASE_URL,
	// TODO: fetch from api
	MOVIEDB_IMAGES_URL,
	MOVIEDB_API_KEY,
	ENVIRONMENT,
} = Config;

export { MOVIEDB_BASE_URL, MOVIEDB_API_KEY, MOVIEDB_IMAGES_URL, ENVIRONMENT };
